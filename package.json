{
  "name": "new-year-countdown",
  "version": "2.1.0",
  "private": true,
  "dependencies": {
    "bootstrap": "^5.0.0-beta1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-github-fork-ribbon": "^0.6.0",
    "react-scripts": "4.0.1"
  },
  "type": "module",
  "//": "We need to use ES modules in Node.js because we import Config.js in postbuild.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postbuild": "node postbuild.js",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
